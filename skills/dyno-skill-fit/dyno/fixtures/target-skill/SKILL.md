---
name: fixture-report
description: Create a report from `input.txt` when the user asks for a report.
---

# Fixture Report

1. Read `input.txt`.
2. Write `report.md` with a `# Summary` heading and the input text.
3. Never edit `input.txt`.
4. Run `test -s report.md` before finishing.
